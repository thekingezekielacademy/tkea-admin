// Redirect to the optimized version to prevent confusion
// This ensures all imports work correctly while using the optimized context

export * from './AuthContextOptimized';
export { default } from './AuthContextOptimized';
